server {
    listen <%= ENV["PORT"] %>;
    server_name _;

    root /app;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /assets/ {
        expires 1y;
        add_header Cache-Control public;
    }
}
